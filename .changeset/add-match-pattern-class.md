---
"@ublacklist/match-pattern": minor
---

Add a `MatchPattern` class that tests a URL against a set of match patterns. It builds a `MatchPatternMap` internally, so it stays efficient for multiple patterns.
