---
"@ublacklist/serpinfo": minor
---

Add an optional `extraSelector` field to a result definition. It takes a CSS selector list where every top-level selector must reference the result root via the nesting selector `&`, validated with css-tree.
